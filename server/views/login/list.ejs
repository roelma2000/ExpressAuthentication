<!---Filename: login.ejs
     Student Name: Rowel Almuete - 301137911 
      Date: 2022-Feb-23 -->
      <%- include ../partials/header %>
      <%- include ../partials/main_nav %>   
        
      <!--- Body Begins-->
         
      <section>
              <div class=" mt-2">
                <div class="container">
                  <div class="row">
                    <div >
                      <h5><%= title %></h5>
                      <% if(typeof displayName=="string" && displayName){%>
                        <p><a href="/login-list/add" class="btn btn-primary" role="button">
                          <i class="fas fa-plus-circle"></i> Add User </a></p>
                     <% }%>

                      <table class="table table-striped table-hover">
                        <thead class="thead-light">
                          <tr>
                            <th scope="col">Username</th>
                            <th scope="col">Password</th>
                            <th scope="col">Email</th>
                            <th scope="col">Address</th>
                          </tr>
                        </thead>
                        <tbody>
                            <% for(let cnt=0;cnt<LoginList.length;cnt++){ %>
                          <tr>
                            <td><%= LoginList[cnt].username%></td>
                            <td><%= LoginList[cnt].password%></td>
                            <td><%= LoginList[cnt].email%></td>
                            <td><%= LoginList[cnt].address%></td>
                            <% if(typeof displayName=="string" && displayName){%>
                            <td>
                              <a href="/login-list/edit/<%= LoginList[cnt]._id %>" class="btn btn-primary btn-sm">
                                <i class="fas fa-pencil-alt"></i> Edit</a>
                            </td>
                            <td>
                              <a href="/login-list/delete/<%= LoginList[cnt]._id %>" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash-alt"></i> Delete</a>
                            </td>
                            <% }%>
                          </tr>
                          <% } %>
                         
                        </tbody>
                      </table>
                    </div>
                    
                    <!---<div class="col-12 col-lg-6">
                      
                   </div>-->
                  <div><p>
                     <!---Last Row  -->
                      
                  </p></div>
                  </div>
                </div>
              </div>

            </section>
            
      <!--- Footer Begins-->
       <%- include ../partials/footer %> 